# mdbook template

Built with [mdBook](https://rust-lang.github.io/mdBook/), and deployed with a GitHub action ([ci.yaml](.github/workflows/ci.yaml)).

To run locally, install `mdBook`, then:

```
~/mdbook serve
```
